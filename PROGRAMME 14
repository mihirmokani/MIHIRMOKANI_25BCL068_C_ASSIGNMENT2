#include <stdio.h>
int main() {
    int sum=0, val;
    printf("Enter 10 values:\n");
    for(int i=1; i<=10; i++){
        scanf("%d", &val);
        sum += val;
    }
    printf("Sum = %d\n", sum);
    printf("Mean = %.2f", sum/10.0);
    return 0;
}
