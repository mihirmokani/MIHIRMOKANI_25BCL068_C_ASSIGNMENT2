#include <stdio.h>
int main() {
    for(int n=2; n<=500; n++){
        int count=0;
        for(int i=1; i<=n; i++)
            if(n%i==0) count++;
        if(count==2) printf("%d ", n);
    }
    return 0;
}
